<b> to search record field_name:value, for a date search date_field_name:start_date,end_date</b>
<p> supported fields description, priority, status and last_modified_at</p>
<p> <b>query eg:</b> status:closed&last_modified_at:2019-01-01,2019-12-01</p>
<%=form_for @issues, :url => { :action => "search" }, :html => { :method => :post, :id => "Updateform" } do |f| %>
Search: <input name="query" value=""/>
</br>
<%= f.submit "search"%>
<%end%>
</br>
</br>
<table>
    <th>description</th>
    <th>priority</th>
    <th>status</th>
    <th>submitted_by</th>
    <th>last_modifed_date</th>
    <th>action</th>
    <% @issues.each do |issue| %>
        <tr>
           <td><%= issue.description %></td>
           <td><%= issue.priority %></td>
           <td><%= issue.status %></td>
           <td><%= issue.submitted_by %></td>
           <td><%= issue.updated_at %></td>
           <td><a href="/issues/<%=issue.id%>/edit">edit</a></td>   
        </tr>
    <%end%>
</table>